
// pdf version
                // var doc = new jsPDF("landscape");
                // doc.setFontSize(18);
                // doc.text('Issues Report', 14, 22);
                // doc.setFontSize(11);
                // doc.setTextColor(100);
                // doc.text("Date: "+formattedTime, 14, 29);
                // doc.setFontSize(10);
                // doc.setTextColor(100);
                // // report format description

                // var pageSize = doc.internal.pageSize;
                // var pageWidth = pageSize.width ? pageSize.width : pageSize.getWidth();
                // var text = doc.splitTextToSize(description, pageWidth - 35, {});
                // doc.text(text, 14, 37);
                // // // You can use html:
                // doc.autoTable({
                //     html: '#report',
                //     tableWidth: 'wrap',
                //     startY: 50,
                //     styles: {cellPadding: 1.5, fontSize: 8,overflow: 'linebreak', cellWidth:'wrap'},
                //     columnStyles: {
                //         1: {
                //             cellWidth : 20
                //         }, 
                //         2: {
                //             cellWidth: 20
                //         },
                //         9 : {
                //             cellWidth : 30
                //         },
                //         10 : {
                //             cellWidth : 30
                //         }
                //     }
                // });    
                // doc.save(`${formattedTime}__${period}__${statusMapper[status]}.pdf`);
